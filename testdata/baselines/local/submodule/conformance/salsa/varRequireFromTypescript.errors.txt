ex.d.ts(3,5): error TS2564: Property 'n' has no initializer and is not definitely assigned in the constructor.


==== ex.d.ts (1 errors) ====
    export type Greatest = { day: 1 }
    export class Crunch {
        n: number
        ~
!!! error TS2564: Property 'n' has no initializer and is not definitely assigned in the constructor.
        m(): number
        constructor(n: number)
    }
    
==== use.js (0 errors) ====
    var ex = require('./ex')
    
    // values work
    var crunch = new ex.Crunch(1);
    crunch.n
    
    
    // types work
    /**
     * @param {ex.Greatest} greatest
     * @param {ex.Crunch} wrap
     */
    function f(greatest, wrap) {
        greatest.day
        wrap.n
    }
