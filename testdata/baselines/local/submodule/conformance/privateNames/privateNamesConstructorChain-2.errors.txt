privateNamesConstructorChain-2.ts(3,12): error TS1005: ';' expected.
privateNamesConstructorChain-2.ts(15,5): error TS2349: This expression is not callable.


==== privateNamesConstructorChain-2.ts (2 errors) ====
    class Parent<T> {
        #foo = 3;
        static #bar = 5;
               ~~~~
!!! error TS1005: ';' expected.
        accessChildProps() {
            new Child<string>().#foo; // OK (`#foo` was added when `Parent`'s constructor was called on `child`)
            Child.#bar;       // Error: not found
        }
    }
    
    class Child<T> extends Parent<T> {
        #foo = "foo";       // OK (Child's #foo does not conflict, as `Parent`'s `#foo` is not accessible)
        #bar = "bar";       // OK
    }
    
    new Parent<number>().accessChildProps();
        ~~~~~~
!!! error TS2349: This expression is not callable.
