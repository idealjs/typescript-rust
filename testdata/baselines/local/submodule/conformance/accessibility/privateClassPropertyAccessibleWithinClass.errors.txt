privateClassPropertyAccessibleWithinClass.ts(6,29): error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
privateClassPropertyAccessibleWithinClass.ts(11,36): error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
privateClassPropertyAccessibleWithinClass.ts(20,37): error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
privateClassPropertyAccessibleWithinClass.ts(26,22): error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.


==== privateClassPropertyAccessibleWithinClass.ts (4 errors) ====
    // no errors
    
    class C {
        private x: string;
        private get y() { return this.x; }
        private set y(x) { this.y = this.x; }
                                ~
!!! error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
        private foo() { return this.foo; }
    
        private static x: string;
        private static get y() { return this.x; }
        private static set y(x) { this.y = this.x; }
                                       ~
!!! error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
        private static foo() { return this.foo; }
        private static bar() { this.foo(); }
    }
    
    // added level of function nesting
    class C2 {
        private x: string;
        private get y() { () => this.x; return null; }
        private set y(x) { () => { this.y = this.x; } }
                                        ~
!!! error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
        private foo() { () => this.foo; }
    
        private static x: string;
        private static get y() { () => this.x; return null; }
        private static set y(x) {
            () => { this.y = this.x; }
                         ~
!!! error TS2339: Property 'y' does not exist on type '{ x: string; foo: () => any }'.
         }
        private static foo() { () => this.foo; }
        private static bar() { () => this.foo(); }
    }
