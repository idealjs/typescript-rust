typeGuardsInForStatement.ts(11,13): error TS2454: Variable 'cond' is used before being assigned.
typeGuardsInForStatement.ts(18,13): error TS2454: Variable 'cond' is used before being assigned.


==== typeGuardsInForStatement.ts (2 errors) ====
    let cond: boolean;
    function a(x: string | number) {
        for (x = undefined; typeof x !== "number"; x = undefined) {
            x; // string
        }
        x; // number
    }
    function b(x: string | number) {
        for (x = undefined; typeof x !== "number"; x = undefined) {
            x; // string
            if (cond) continue;
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
        }
        x; // number
    }
    function c(x: string | number) {
        for (x = undefined; typeof x !== "number"; x = undefined) {
            x; // string
            if (cond) break;
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
        }
        x; // string | number
    }
