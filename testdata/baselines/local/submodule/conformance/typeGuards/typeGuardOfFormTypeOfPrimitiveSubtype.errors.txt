typeGuardOfFormTypeOfPrimitiveSubtype.ts(3,12): error TS2454: Variable 'a' is used before being assigned.
typeGuardOfFormTypeOfPrimitiveSubtype.ts(4,21): error TS2454: Variable 'a' is used before being assigned.


==== typeGuardOfFormTypeOfPrimitiveSubtype.ts (2 errors) ====
    let a: {};
    let b: {toString(): string};
    if (typeof a === "number") {
               ~
!!! error TS2454: Variable 'a' is used before being assigned.
        let c: number = a;
                        ~
!!! error TS2454: Variable 'a' is used before being assigned.
    }
    if (typeof a === "string") {
        let c: string = a;
    }
    if (typeof a === "boolean") {
        let c: boolean = a;
    }
    
    if (typeof b === "number") {
        let c: number = b;
    }
    if (typeof b === "string") {
        let c: string = b;
    }
    if (typeof b === "boolean") {
        let c: boolean = b;
    }
