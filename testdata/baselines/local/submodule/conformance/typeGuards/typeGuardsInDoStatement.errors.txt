typeGuardsInDoStatement.ts(14,13): error TS2454: Variable 'cond' is used before being assigned.
typeGuardsInDoStatement.ts(23,13): error TS2454: Variable 'cond' is used before being assigned.


==== typeGuardsInDoStatement.ts (2 errors) ====
    let cond: boolean;
    function a(x: string | number | boolean) {
        x = true;
        do {
            x; // boolean | string
            x = undefined;
        } while (typeof x === "string")
        x; // number | boolean
    }
    function b(x: string | number | boolean) {
        x = true;
        do {
            x; // boolean | string
            if (cond) continue;
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x = undefined;
        } while (typeof x === "string")
        x; // number | boolean
    }
    function c(x: string | number) {
        x = "";
        do {
            x; // string
            if (cond) break;
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x = undefined;
        } while (typeof x === "string")
        x; // string | number
    }
