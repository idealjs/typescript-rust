typeGuardOfFormThisMemberErrors.ts(4,3): error TS2564: Property 'isFSO' has no initializer and is not definitely assigned in the constructor.
typeGuardOfFormThisMemberErrors.ts(14,3): error TS2564: Property 'isNetworked' has no initializer and is not definitely assigned in the constructor.
typeGuardOfFormThisMemberErrors.ts(19,55): error TS2349: This expression is not callable.
typeGuardOfFormThisMemberErrors.ts(22,3): error TS2564: Property 'children' has no initializer and is not definitely assigned in the constructor.


==== typeGuardOfFormThisMemberErrors.ts (4 errors) ====
    // There's a 'File' class in the stdlib, wrap with a namespace to avoid collision
    namespace Test {
    	export class FileSystemObject {
    		isFSO: this is FileSystemObject;
    		~~~~~
!!! error TS2564: Property 'isFSO' has no initializer and is not definitely assigned in the constructor.
    		get isFile(): this is File {
    			return this instanceof File;
    		}
    		set isFile(param) {
    			// noop
    		}
    		get isDirectory(): this is Directory {
    			return this instanceof Directory;
    		}
    		isNetworked: this is (Networked & this);
    		~~~~~~~~~~~
!!! error TS2564: Property 'isNetworked' has no initializer and is not definitely assigned in the constructor.
    		constructor(public path: string) {}
    	}
    
    	export class File extends FileSystemObject {
    		constructor(path: string, public content: string) { super(path); }
    		                                                    ~~~~~
!!! error TS2349: This expression is not callable.
    	}
    	export class Directory extends FileSystemObject {
    		children: FileSystemObject[];
    		~~~~~~~~
!!! error TS2564: Property 'children' has no initializer and is not definitely assigned in the constructor.
    	}
    	export interface Networked {
    		host: string;
    	}
    
    	let file: FileSystemObject = new File("foo/bar.txt", "foo");
    	file.isNetworked = file.isFile;
    	file.isFSO = file.isNetworked;
    	file.isFile = file.isFSO;
    }
