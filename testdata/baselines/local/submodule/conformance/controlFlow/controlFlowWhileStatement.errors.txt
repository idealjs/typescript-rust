controlFlowWhileStatement.ts(5,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(12,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(21,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(31,18): error TS2339: Property 'length' does not exist on type '""'.
controlFlowWhileStatement.ts(39,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(49,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(50,13): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(54,13): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(66,13): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(70,13): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(92,12): error TS2454: Variable 'cond' is used before being assigned.
controlFlowWhileStatement.ts(101,12): error TS2454: Variable 'cond' is used before being assigned.


==== controlFlowWhileStatement.ts (12 errors) ====
    let cond: boolean;
    function a() {
        let x: string | number;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x; // string
        }
    }
    function b() {
        let x: string | number;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x; // string
            x = 42;
            break;
        }
    }
    function c() {
        let x: string | number;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x; // string
            x = undefined;
            if (typeof x === "string") continue;
            break;
        }
    }
    function d() {
        let x: string | number;
        x = "";
        while (x = x.length) {
                     ~~~~~~
!!! error TS2339: Property 'length' does not exist on type '""'.
            x; // number
            x = "";
        }
    }
    function e() {
        let x: string | number;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x; // string | number
            x = 42;
            x; // number
        }
        x; // string | number
    }
    function f() {
        let x: string | number | boolean | RegExp | Function;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            if (cond) {
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
                x = 42;
                break;
            }
            if (cond) {
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
                x = true;
                continue;
            }
            x = /a/;
        }
        x; // string | number | boolean | RegExp
    }
    function g() {
        let x: string | number | boolean | RegExp | Function;
        x = "";
        while (true) {
            if (cond) {
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
                x = 42;
                break;
            }
            if (cond) {
                ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
                x = true;
                continue;
            }
            x = /a/;
        }
        x; // number
    }
    function h1() {
        let x: string | number | boolean;
        x = "";
        while (x > 1) {
            x; // string | number
            x = 1;
            x; // number
        }
        x; // string | number
    }
    declare function len(s: string | number): number;
    function h2() {
        let x: string | number | boolean;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x = len(x);
            x; // number
        }
        x; // string | number
    }
    function h3() {
        let x: string | number | boolean;
        x = "";
        while (cond) {
               ~~~~
!!! error TS2454: Variable 'cond' is used before being assigned.
            x; // string | number
            x = len(x);
        }
        x; // string | number
    }
