# 修复计划（r21 后轮）— 2026-08-24

前置：r21 全量 sweep（验证 r20-fix 残留：TS2305 shorthand 豁免 + 顺序测试修正 +
上一会话已实现的 Fix 1 TS2305 子系统 / Fix 3 tsconfig 单元）。单测四套件全绿
（1,344/921/15/2，新增 shorthand_ambient_module_members_exempt_from_2305）。

r20 遗留清单（r21 验证后在 log 确认构成）：
- compiler：recursiveReverseMappedType（深层延迟条件/映射）
- conformance：logicalAssignment5×4、Emit1×4（TS2305，Fix 1 应清）、
  library-reference-13（tsconfig 单元，Fix 3 应清）、templateLiteralTypes1（深层）、
  genericClassWith（D3，本轮已归因——见 Fix 5）

## Fix 4：logicalAssignment5（r15 计划 A + r20 计划 B 合并，CLI 已实证四缺口）

官方基线（esnext 配置，4 错）：foo1/foo2 干净；foo3 `f(42)`→2722；bar1/bar2
RHS 内 `f.toString()`→18048（对象位置）；bar3 `f(42)`→2722。

CLI 探针实证（/tmp/probe21c/la5.ts，我方 0 错 vs 官方 4 错）：

1. **assignment_flow_type 补逻辑赋值后置窄化**（flow.rs ~3164，现仅 EqualsToken）：
   - `??=`：nonNullish(declared) | RHS 型；`||=`：truthy 叶 | RHS 型；
     `&&=`：falsy 叶(含 undefined) | RHS 型 → foo3/bar3 的 2722 由既有
     CANNOT_INVOKE_OBJECT_POSSIBLY_UNDEFINED 路径（checker.rs ~6133）报出
2. **RHS 区域窄化帧**：check_assignment_compat（checker.rs ~13190）对
   ??=/||= 在 get_type_of_node(right) 前推帧（目标符号 → nullish/falsy 窄化），
   &&= 推 truthy 帧；get_flow_type_of_reference 流走无窄化时套用帧。
   bar1/bar2 的 f 在 RHS 内变 undefined → 触发 3 的 18048
3. **18048 门控重做**（check_property_access ~5951）：现要求
   property_exists_on_non_nullable_part 才报 → 改为 Go checkNonNullType 语义：
   possibly-undefined/null 即在**对象表达式位置**报（现错误地用 name.loc），
   之后在 nonNullable 部分继续属性查找。probe：`f?.m()`（?. 豁免保持）
4. **Object 接口成员回退**（Go getPropertyOfTypeEx ~18992 尾部）：
   结构化对象类型属性查找失败 → 回退全局 Object 接口成员
   （globals["Object"] interface members）。`o.toString()` 现误报 2339。
   注意影响面：可能清一批 accepted-diff、也可能翻转个别钉住的用例——
   修后单测全量 + r22 验证

子顺序：4 → 3 → 1 → 2（3 依赖 4，否则 18048 后仍跟假 2339）。

单元测试（node_format_tests 风格，check_files）：
- la5 六函数全形态：codes == [2722, 18048, 18048, 2722]（esnext）
- `{m():void}|undefined` 的 `.m()` / `.toString()` → 仅 18048（无 2339）
- 非空对象字面量 `.toString()` / `.valueOf()` 零错误

## Fix 5：genericClassWith（D3 归因已定）

我方多报 2×2345（(28,19)/(29,20)）：`g.foo(c1, d1)`，
`G<T extends {x:string}>` 的方法 `foo<T extends {x:string}>(t: X<T>, t2: X<T>): T`
——我们解析方法签名时把**接收者的类型实参 {x:string;y:string}** 代入了方法
自有 T（应为方法自己的推断：候选 C、D 均满足约束 → 公共超类型/约束内推断，
官方零错误）。修法：方法调用签名实例化不得让 receiver mapper 覆盖方法自有
类型参数（binder 同名 T 符号合并的已知怪癖——A7 的 type_argument_name_frames
按名并行帧就是这个坑；调用点侧需要同样的按符号身份区分）。

单元测试：上方 case 最小化（class G + 方法重声明 T + 双 X<T> 实参推断）。

## Fix 6：templateLiteralTypes1 / recursiveReverseMappedType（深层，本轮若有余力）

- template1 额外错：`(35,9)` 模板字面量 `get${error}` vs 联合字面量（pattern
  匹配推断）；`(39,40)` 延迟映射类型 `{[error in string]: T[P]}` 同一性；
  `(152-156, 237)` 深层泛型模板递归（`extends string ? ...` 链）
- recursiveReverse：Recur<T> 双侧延迟同一性比较（自由类型参数保持 deferred，
  官方两侧同型恒真）。归因：is_type_related_to 对 deferred conditional 的
  双侧同为同一别名实例时未走同一性短路

## Fix 1b：TS2305/2459 假阳性（r21 compiler 段新 FAIL ×4，紧急）

r21 首批回归（r20 后写入的 Fix 1 首次上全量）：
- constEnumNoEmitReexport / constEnumPreserveEmitNamedExport1/2：`export { A }`
  （本地导出子句，非 inline export）——binder 把别名 A 放进容器表但
  module_member_lookup 只认 export_symbol 自链 → 误报 2459
- assertionFunctionWildcardImport1：`export * from "../../core/..."` 星导出
  barrel——目标模块经星链导出的成员不可见 → 误报 2459/2305

修法（checker 侧 module_member_lookup 扩展）：
1. 名字命中容器 locals/members 的 **Alias 符号且声明为 ExportSpecifier**
   → Found（本地 `export {A}` 与 `export {A} from "m"` 都在此形态）
2. 文件的每个 `export * from` 声明（module_symbol.exports 的 "export*" 条目
   的声明节点，取 module_specifier 相对该文件解析）→ 递归 module_member_lookup
   （深度上限 8，环回退 Missing→跳过该链）

单元测试：`export {A}` 本地子句、`export {A} from "./m"` 转发、`export * from`
barrel 两跳、三者混合缺名才报 2305。

## Fix 7：r21 新回归簇（非 2305 族；CLI 确定性复现 ×3）

r20 全过、r21 挂、当前树 CLI 稳定复现（/tmp/probe21c/{cpic,cti}.ts，5/5 次）。
post-r20 改动仅四个文件：checker.rs（Fix 1）、inference.rs 02:35（括号透传 +
`||`/`??`/`&&`/逗号上下文定型重做 = Fix 2A 部分）、tsoptions 02:21（with_base
拆分，低风险）、harness 02:22（Fix 3，无 tsconfig 单元时无行为变化）。
二分顺序：先 revert inference.rs 上下文重做 → 重建探针；再查 checker.rs Fix 1
块。三个复现：
- classPropertyInferenceFromBroaderTypeConst：`static SD = DEFAULT`（const
  DEFAULT: AB='A'）推成 'A' 非 AB → `D.SD='B'` 假 2322
- cyclicTypeInstantiation：`a = b`（foo<string>() vs bar<string>() 循环
  匿名类型）假 2719——显示 {a: T; b: any}（T 未替换，b 变 any），
  实例化未代入且两侧按声明态比较
- controlFlowOuterVariable：`const CONFIG = {foo: ''}` 的 foo 属性未加宽
  （'""' 非 string）→ `CONFIG.foo = foo` 假 2322（@strictNullChecks: true）

三者共性：字面量/实例化的**加宽与代入**路径。若二分指向 inference.rs 的
`&&`/逗号重做（对象字面量属性初始化器经由逗号/&& 形态？），恢复逐形态。

## 顺序

Fix 1b（回归止血，最高优先）→ Fix 7（回归簇二分）→ Fix 4（4a→4b→4c→4d→4e→4f）
→ Fix 5 → Fix 6。r21 log 落地后先核对各 stem 实际构成再动手
（Emit1/library-reference-13 预期已被上一会话的 Fix 1/Fix 3 清零）。
